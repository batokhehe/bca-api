name: my_project
description: A new Dart Frog application
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  basic_utils: ^5.8.2
  dart_frog: ^1.2.0
  timezone: ^0.10.0
  http: ^1.3.0
  logger: ^2.0.2  # or the latest version
  path: ^1.8.3

dev_dependencies:
  mocktail: ^1.0.4
  test: ^1.25.15
  very_good_analysis: ^7.0.0
